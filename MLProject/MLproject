name: spam_model_retrain

conda_env: MLProject/conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: MLProject/emails_preprocessed.csv}
    command: >
      python MLProject/modelling.py --data_path {data_path}
